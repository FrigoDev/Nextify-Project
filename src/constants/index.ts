export enum Pages {
  HOME = "/",
  LOGIN = "/login",
  PLAYLIST = "/playlists",
  ARTIST = "/artists",
  ALBUM = "/albums",
  SEARCH = "/search",
  TRACKS = "/tracks",
  LIBRARY = "/library",
}

export enum Assets {
  LOGO = "/assets/images/SpotifyLogo.png",
  DEFAULT_IMAGE = "/assets/images/SpotifyDefaultImage.png",
}
